const fetch = require('node-fetch'); // Ensure node-fetch is available
const fs = require('fs');

async function testHF() {
  const apiKey = 'YOUR_HF_KEY'; // We will use process.env in real
  // ...
}
testHF();
